# Projects API – Overview

이 문서는 **프로젝트(Projects) API** 전체 개요와 엔드포인트 목록을 정의한다.

---

## 1. 프로젝트 시스템 개요

- **목적**: Lucent 레이블이 진행하는 프로젝트 정보를 제공
- **공개 범위**: 모든 프로젝트는 공개 정보 (인증 불필요)
- **컨텐츠**: 프로젝트 설명, 이미지, 관련 아티스트 목록
- **구조**: 하나의 프로젝트에 여러 아티스트가 속할 수 있음

---

## 2. 프로젝트 개념

### 2.1 프로젝트란?

- 레이블이 기록하는 활동 단위
- 예시:
  - 신곡 발매
  - 콜라보레이션
  - 이벤트 개최
  - 굿즈 출시
  - 라이브 방송

### 2.2 프로젝트와 아티스트

- **프로젝트**: 레이블의 활동을 기록하는 단위 (예: 0th, 1st)
- **아티스트**: 프로젝트에 속하는 버츄얼 캐릭터 (예: 미루루, Drips)
- 하나의 프로젝트에 여러 아티스트가 속할 수 있음

📄 아티스트 상세: `specs/api/artists/`

### 2.3 프로젝트 vs 상품

- **프로젝트**: 레이블의 활동 기록 (정보 제공 목적)
- **상품**: 판매 가능한 굿즈 (구매 목적)
- 상품은 아티스트에 속하며, 프로젝트를 통해 간접적으로 연결됨

---

## 3. API 엔드포인트 목록

| Method | Endpoint                 | 설명             | 인증 필요 | 스펙 문서  |
| ------ | ------------------------ | ---------------- | --------- | ---------- |
| GET    | `/api/projects`          | 프로젝트 목록    | ❌        | list.md    |
| GET    | `/api/projects/:slug`    | 프로젝트 상세    | ❌        | detail.md  |

---

## 4. 데이터 모델

### 4.1 Project 기본 구조 (DB 스키마 기반)

```ts
interface Project {
  id: string;
  name: string;
  slug: string; // URL에 사용 (예: '0th', '1st')
  description: string;

  // 이미지 관리 (images 테이블 참조)
  cover_image: {
    id: string;
    public_url: string;
    cdn_url?: string;
    alt_text?: string;
    width?: number;
    height?: number;
  } | null;

  order_index: number; // 표시 순서
  is_active: boolean;
  created_at: string;
  updated_at: string;

  // 관련 아티스트 (JOIN)
  artists?: ArtistPreview[];
}
```

### 4.2 ArtistPreview 구조

```ts
interface ArtistPreview {
  id: string;
  name: string;
  slug: string;
  profile_image: {
    public_url: string;
    cdn_url?: string;
  } | null;
}
```

📄 아티스트 전체 구조: `specs/api/artists/`

### 4.3 ProductPreview 구조

```ts
interface ProductPreview {
  id: string;
  name: string;
  slug: string;
  type: 'VOICE_PACK' | 'PHYSICAL_GOODS';
  price: number;

  // 메인 이미지 (images 테이블 참조)
  main_image: {
    public_url: string;
    cdn_url?: string;
    thumbnail_url?: string;
  } | null;

  stock?: number; // null이면 무제한
  is_active: boolean;
}
```

---

## 5. 공통 쿼리 파라미터

### 5.1 Pagination

```
?page=1&limit=12
```

- `page`: 페이지 번호 (기본: 1)
- `limit`: 페이지당 항목 수 (기본: 12, 최대: 50)

### 5.2 Sorting

```
?sortBy=order_index&order=asc
```

- `sortBy`: 정렬 기준 (기본: `order_index`)
  - `order_index`: 표시 순서 (관리자가 지정한 순서)
  - `created_at`: 생성일
  - `name`: 이름 (가나다순)
- `order`: 정렬 순서 (기본: `asc`)
  - `asc`: 오름차순
  - `desc`: 내림차순

### 5.3 Filtering

```
?filter[artist]=miruru&filter[is_active]=true
```

- `filter[is_active]`: 활성화 여부 (기본: `true`)
- `filter[artist]`: 아티스트 slug (해당 아티스트가 포함된 프로젝트)

---

## 6. 응답 형식

### 6.1 목록 응답

```json
{
  "status": "success",
  "data": [...],
  "pagination": {
    "total": 24,
    "page": 1,
    "limit": 12,
    "totalPages": 2
  }
}
```

### 6.2 상세 응답

```json
{
  "status": "success",
  "data": { ... }
}
```

---

## 7. 1차 MVP 범위

### 포함

- ✅ 프로젝트 목록 조회 (페이지네이션, 정렬, 필터링)
- ✅ 프로젝트 상세 조회
- ✅ 아티스트별 필터링
- ✅ 이미지 중앙 관리 (images 테이블 참조)
- ✅ 관련 아티스트 정보 포함

### 제외 (2차 확장)

- ⏸️ 프로젝트 검색 (전문 검색)
- ⏸️ 프로젝트 좋아요/북마크
- ⏸️ 프로젝트 댓글
- ⏸️ 프로젝트 공유 기능
- ⏸️ 프로젝트 상태 관리 (upcoming/ongoing/completed)
- ⏸️ SNS 링크, 태그 시스템

---

## 8. 확장 고려사항

- 프로젝트 타임라인 뷰
- 아카이브 기능 (연도별, 아티스트별)
- 프로젝트 알림 설정
- 프로젝트 구독 기능
- 외부 컨텐츠 임베드 (YouTube, Twitter 등)
